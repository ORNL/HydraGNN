# Base HydraGNN dependencies
# Scientific computing, materials science, visualization, and utility packages
# Install with: pip install -r requirements-base.txt
matplotlib==3.10.6
ase==3.26.0
tqdm==4.70.0
tensorboard==2.20.0
psutil==7.1.0
sympy==1.14.0
matscipy
scikit-learn==1.7.2
numpy==2.2.6
scipy==1.17.1
mpi4py==4.1.1
vesin==0.4.2
# Required by the vendored FairChem UMA backbone
# (hydragnn/utils/model/uma/_vendored). Lightweight; the heavier fairchem-core
# deps (torchtnt/ray/wandb/hydra) are only reached in checkpoint/inference code
# paths that HydraGNN does not use, so they are deferred to lazy imports.
omegaconf==2.3.0
monty>=2024.7.30
