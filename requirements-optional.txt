pymatgen
jarvis-tools # needed for MPTrj dataset
optuna # hyperparameter optimization
deephyper # hyperparameter optimization
h5py==3.16.0 # utils to read hdf5 files
p7zip # to uncompress .7z file after download - needed for qm7x example
lmdb # needed to use FAIRChem functionalities released by the Meta Research AI group
orjson # needed to use FAIRChem functionalities released by the Meta Research AI group
pandas==2.3.3 # needed for HPO examples and FAIRChem functionalities
PyYAML # needed for FAIRChem configuration files
wandb # needed for FAIRChem logging functionalities

# --- Vendored FAIRChem UMA backbone runtime deps -------------------------------
# Unlike the entries above (which are truly optional and NOT installed in CI),
# these two are imported by the vendored UMA backbone whenever a UMA model runs,
# and UMA is exercised in CI. They are therefore installed by a dedicated CI
# step (.github/workflows/CI.yml) that keeps pip build isolation ENABLED, so
# omegaconf's sdist-only antlr4-python3-runtime dependency can build its own
# backend -- installing them under the base `--no-build-isolation` step fails.
omegaconf # UMA backbone: DictConfig/ListConfig config types
monty # UMA backbone: monty.dev.requires decorator

